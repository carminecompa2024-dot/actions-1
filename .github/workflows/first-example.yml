name: My First workflow

on: push

jobs:
    first_job:
       runs-on: ubuntu-latest
       steps:
        - name: Checkout Repo
          uses: actions/checkout@v6

        - name: List files
          run: ls -ltra
        
        - name: Executing shell script 
          run: ./ascii-script.sh  

     

        #- name: Read file
        #  run: cat README.md 